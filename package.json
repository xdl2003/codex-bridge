{
  "name": "codex-bridge",
  "version": "0.1.0",
  "description": "Local CLI, HTTP API, and web UI bridge for resuming Codex CLI sessions by workspace.",
  "type": "commonjs",
  "bin": {
    "codex-bridge": "./bin/codex-bridge.js"
  },
  "scripts": {
    "start": "node ./bin/codex-bridge.js",
    "server": "node ./bin/codex-bridge.js server",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
